export class User {
  /**
   * The name of the user
   */
  name: string;

  /**
   * The email of the user
   */
  email: string;

  /**
   * The role of the user,
   * this can be 'USER' or 'WORKER'
   */
  role: string;
}
